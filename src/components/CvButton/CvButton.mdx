import { Canvas, Meta } from '@storybook/blocks';
import * as CvButtonStories from './CvButton.stories';
import * as CvButtonSetStories from './CvButtonSet.stories';
import * as CvButtonSkeletonStories from './CvButtonSkeleton.stories';
import * as CvIconButtonStories from './CvIconButton.stories';

<Meta of={CvButtonStories} />

# CvButton

A button triggers an event or action. They let users know what will happen next.

## Kinds

<Canvas of={CvButtonStories.Primary} />
<Canvas of={CvButtonStories.Secondary} />

## Sizes

<Canvas of={CvButtonStories.Field} />
<Canvas of={CvButtonStories.Small} />
<Canvas of={CvButtonStories.Large} />

# CvIconButton

Icon-only buttons use icons in place of text labels. They must include an accessible `label` prop used as a tooltip.

<Canvas of={CvIconButtonStories.Primary} />
<Canvas of={CvIconButtonStories.Secondary} />
<Canvas of={CvIconButtonStories.Field} />
<Canvas of={CvIconButtonStories.Small} />
<Canvas of={CvIconButtonStories.Large} />

# CvButtonSet

Group related actions in a button set. Use the `stacked` prop to arrange buttons vertically.

<Canvas of={CvButtonSetStories.Default} />

# CvButtonSkeleton

Use the skeleton variant while button content is loading.

<Canvas of={CvButtonSkeletonStories.Default} />
<Canvas of={CvButtonSkeletonStories.Field} />
<Canvas of={CvButtonSkeletonStories.Small} />
<Canvas of={CvButtonSkeletonStories.Large} />
