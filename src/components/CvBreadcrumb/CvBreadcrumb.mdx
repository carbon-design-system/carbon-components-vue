import { Canvas, Meta } from '@storybook/blocks';
import * as CvBreadcrumbStories from './CvBreadcrumb.stories';

<Meta of={CvBreadcrumbStories} />

# CvBreadcrumb

A breadcrumb is a secondary navigation pattern that shows hierarchy among content or traces a user's path.

<Canvas of={CvBreadcrumbStories.Default} />

# Skeleton

Use the skeleton variant while the breadcrumb content is loading.

<Canvas of={CvBreadcrumbStories.Skeleton} />
